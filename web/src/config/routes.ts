/**
 * Centralized route path constants (without locale prefix).
 * Use `toLocalizedPath(ROUTES.X)` to produce locale-aware URLs.
 */
export const ROUTES = {
  HOME: '/',
  LEARN: '/learn',
  FIRST_LESSON: '/learn/foundations/perceptron/weighted-sum',
} as const
